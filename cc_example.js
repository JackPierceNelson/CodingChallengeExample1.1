// Task 1: Variables and Data Types

let customerName = "Bob Marley";
const customerID = 1234;
var isPremiumMember = false;

// logging each variable's value and type to the console
console.log(customerName, typeof(customerName));
console.log(customerID, typeof(customerID));
console.log(isPremiumMember,typeof(isPremiumMember));

// Task 2: Compound Data Types

let orderItems = ["Computer", "Laptop", "Macbook"]; // Declaring an array to store listed items
const orderDetails = {   
    orderID: 123, 
    items: orderItems,
    totalAmount: 2459.00
                    } // Creating an object to represent an order with properties like OrderID, items, and totalAmount

console.log(orderItems, orderDetails); // logging orderItems and orderDetails to the console

